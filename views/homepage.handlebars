<div class="container">
  {{#each posts}}
    <div class="post">
      <h2>{{this.title}}</h2>
      <p>{{this.content}}</p>
      <div class="meta">
        <span>Posted by {{this.user.username}} on {{this.createdAt}}</span>
      </div>
    </div>
  {{/each}}
</div>
